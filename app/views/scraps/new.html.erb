<h1>Post a new scrap:</h1>
<%= form_for @scrap do |f| %>
	<%= f.text_field :image_url, placeholder: "Image Url..." %>
	<%= f.text_field :caption, placeholder: "Caption... "%>
	<%= f.submit "Submit" %>
<% end %>
